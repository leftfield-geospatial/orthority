name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * 1'  # run weekly

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: [ '3.10', '3.14' ]
        os: [ macos-latest, ubuntu-latest, windows-latest ]

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest pytest-cov pytest-xdist
          python -m pip install rasterio "opencv-python-headless<5" numpy click tqdm pyyaml fsspec requests aiohttp threadpoolctl
          python -m pip list

      - name: Test with pytest
        timeout-minutes: 5
        run: |
          python -m pytest -vv -n auto --cov=orthority --cov-report=term-missing --cov-report=xml:coverage.xml ./tests

      - name: Upload coverage
        uses: codecov/codecov-action@v7
        with:
          fail_ci_if_error: true
          files: ./coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true
